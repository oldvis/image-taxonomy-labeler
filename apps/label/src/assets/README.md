# Assets

App-local helpers for the labeling interface.

The visualization catalog (`visualizations.json`) lives in
[`packages/shared/assets`](../../../../packages/shared/assets/) and is consumed via
`@image-taxonomy-labeler/shared`. To refresh it, run `packages/shared/assets/download.py`.
