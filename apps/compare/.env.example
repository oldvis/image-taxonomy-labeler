# Backend origin (no trailing slash)
VITE_API_BASE=http://localhost:5001

# true | false | unset
# unset => enable services only when the page is on localhost or 127.0.0.1
# false => static demo mode (remote image URLs from metadata)
# true  => always call the API (requires CORS + reachable server)
VITE_USE_SERVICES=
